# Database - Ganti dengan kredensial PostgreSQL Anda
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/blajarplus?schema=public"

# JWT
JWT_SECRET="blajarplus-secret-key-2024"
JWT_EXPIRES_IN="7d"

# Google OAuth (opsional untuk saat ini)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_CALLBACK_URL="http://localhost:3000/api/auth/google/callback"

# Redis (opsional untuk saat ini)
REDIS_HOST="localhost"
REDIS_PORT=6379
REDIS_PASSWORD=""

# Midtrans (opsional untuk saat ini)
MIDTRANS_SERVER_KEY=""
MIDTRANS_CLIENT_KEY=""
MIDTRANS_IS_PRODUCTION=false

# App
PORT=3000
NODE_ENV="development"
FRONTEND_URL="http://localhost:3001"

# File Upload
MAX_FILE_SIZE=10485760
UPLOAD_DEST="./uploads"

# Email (opsional untuk saat ini)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER=""
SMTP_PASSWORD=""
EMAIL_FROM="Blajarplus <noreply@blajarplus.com>"
